/*!
 * \file sdd.cpp
 *
 * \author cyy
 * \date 2018-03-04
 */

#include "sdd.hpp"
#include "../exception.hpp"

namespace cyy::compiler {

  /*
 void SDD::add_synthesized_attribute( const &production_type production , const std::string & attr_name , const std::vector<std::string> &argument_attr_names,
     std::function< attribute::value_type( const    std::vector<attribute::value_type>   &  )  >
    if(!cfg->has_production(production)) {
      throw unexisted_production();
    }
  }
  */
} 
